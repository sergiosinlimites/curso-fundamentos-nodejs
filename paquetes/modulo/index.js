// Traer nuestro módulo y ejecutar una función del módulo
// Si es solo 1 cosa se puede llamar directamente a 'modulo', al ser más es modulo.loquesea
const modulo = require('./modulo');

// console.log(modulo);
modulo.saludar();
console.log(modulo.prop1);